{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-client": "cd client  && npm install && npm run build",
    "build-nodeClient": "cd nodeClient && npm install",
    "heroku-postbuild": "concurrently \"npm run build-client\" \"npm run build-nodeClient\"",
    "server": "node server.js",
    "nodeClient": "cd nodeClient && node index.js",
    "start": "concurrently \"npm run server\" \"npm run nodeClient\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.0.0",
    "dotenv": "^8.2.0",
    "farmhash": "^3.0.0",
    "helmet": "^3.21.2",
    "mongoose": "^5.7.7",
    "nodemon": "^1.19.4",
    "socket.io": "^2.3.0",
    "socket.io-redis": "^5.2.0"
  }
}